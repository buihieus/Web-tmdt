mixin product-item(item)
  article.product-card
    .product-images-container
      .discount-badge -#{item.discountPercentage}% 
      img.product-images(
        loading="lazy"
        alt=item.title
        src=item.thumbnail
      )
      button.quick-view-btn(aria-label=`Quick view ${item.title}`) Quick View
    .product-info
      if parseInt(item.featured) == 1
        .featured-badge Nổi bật
      h3.product-title #{item.title}
      .price-container
        span.product-price(aria-label=`Current price: $${Math.ceil(item.price * (100 - item.discountPercentage) / 100)} dollars`) $#{Math.ceil(item.price * (100 - item.discountPercentage) / 100)}
        span.original-price(aria-label=`Original price: $${item.price} dollars`) $#{item.price}
      form(
        action=`/cart/add/${item.id}`
        method="POST"
      )
        button.add-to-cart-btn(type="submit" aria-label=`Add ${item.title} to cart`)
          span.btn-text Thêm vào giỏ hàng
          span.btn-icon 🛒

